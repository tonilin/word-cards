
<%= simple_form_for @word, :url => cards_path do |f| %>
  <%= f.input :content %>
  <%= f.submit %>
<% end %>

<table class="table">
  <tr>
    <th>ID</th>
    <th>Word</th>
    <th>Pos</th>
    <th>Explanation</th>
  </td>

  <% @cards.each do |card| %>
  <tr>
    <td><%= card.id %></td>
    <td><%= render_word_content(card.word) %></td>
    <td><%= render_explanation_pos(card.explanation) %></td>
    <td><%= render_explanation_content(card.explanation) %></td>
  </tr>
  <% end %>

</table>